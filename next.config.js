/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['links.papareact.com', 'www.arsenal.com', 'platform-lookaside.fbsbx.com', 'https://images.unsplash.com/']
    }
}

module.exports = nextConfig
